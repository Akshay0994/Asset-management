# Required for production builds — workspace sign-in password.
VITE_ADMIN_PASSWORD=change-me-to-a-strong-password

# Optional — use when hosting under a sub-path (e.g. /assettrack/).
# VITE_BASE_PATH=/assettrack/
